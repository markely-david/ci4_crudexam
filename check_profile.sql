-- Query to view all user profile data
SELECT 
    id,
    fullname,
    username,
    student_id,
    course,
    year_level,
    section,
    phone,
    address,
    profile_image,
    created_at,
    updated_at
FROM users;
